<table class="table table-striped">
  <tr>
    <th>Device</th>
    <th>Category</th>
    <th>Name</th>
    <th>Ip</th>
    <th>Service</th>
    <th>Port</th>
    <th>User</th>
    <th>Password</th>
    <th>Sysadm</th>
    <th>Password Root</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @servers.each do |server| %>
  <tr>
    <td><%= server.device.name %></td>
    <td><%= server.category.name %></td>
    <td><%= server.name %></td>
    <td><%= server.ip %></td>
    <td><%= server.service.name %></td>
    <td><%= server.port %></td>
    <td><%= server.user %></td>
    <td><%= server.password %></td>
    <td><%= server.sysadm ? "SIM" : "NÃO"%></td>
    <td><%= server.root_password %></td>

    <td><%= link_to '', server, :class => "icon-eye-open" %></td>
    <td><%= link_to '', edit_server_path(server), :class => "icon-edit" %></td>
    <td><%= link_to '', server, :class => "icon-remove", method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= button_to 'New Server', new_server_path, :class => "btn  btn-primary", :method => :get %>
<%= button_to "Print", "", :class => "btn btn-primary pull-right", :onclick => "window.print(); return false;", :title => "Clique para Imprimir ou pressione as teclas 'Ctrl + P'" %>

