<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Log in - Estoque ITAI</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Le styles -->
    <%= stylesheet_link_tag "application", :media => "all" %>

    <style type="text/css">
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
        /*padding-top: 40px;*/
        padding-bottom: 40px;
        background-color: #f5f5f5;
      }

      .form-signin {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #fff;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
        margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
      }

      #footer {
        text-align: center;
      }

      .navbar .container {
        font-family: "Helvetica";
        text-align: center;
        line-height: 45px;
        font-size: 23px;
      }

    </style>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>

  <body>

    <div class="navbar navbar-fixed-top navbar-inner">
      <div class="container">
        PMS<strong> Password Management System</strong> <span class="label label-inverse">v1.0</span>
      </div>
    </div>

    <div class="container">
      <%= form_tag sessions_path, class: "form-signin" do %>
        <h2 class="form-signin-heading">Login</h2>
        <% if flash[:notice] %>
          <div class="alert alert-error">
            <%= flash[:notice] %>
          </div>
        <% end %>
        <%= text_field_tag "username", nil, class: "input-block-level", placeholder: "Usuário" %>
        <%= password_field_tag "password", nil, class: "input-block-level", placeholder: "Senha" %>
        <%= submit_tag "Entrar", class: "btn btn-large btn-primary" %>
      <% end %>

    </div> <!-- /container -->

    <div id="footer">
      <!--<p><%#= image_tag "itai2.png", alt: "ITAI" %></p>-->
    </div>
  </body>
</html>